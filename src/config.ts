import "dotenv/config";
import { z } from "zod";
const bool=z.string().default("true").transform(v=>v.toLowerCase()==="true");
const schema=z.object({BOT_NAME:z.string().default("ZipZapBot"),PREFIX:z.string().default("!"),PORT:z.coerce.number().default(8123),TIMEZONE:z.string().default("America/Sao_Paulo"),GROUPS_ENABLED:bool,PANEL_USER:z.string().min(1),PANEL_PASSWORD:z.string().min(10),AI_PROVIDER_ORDER:z.string().default("groq,gemini,openrouter"),GROQ_API_KEY:z.string().optional(),GROQ_MODEL:z.string().default("llama-3.3-70b-versatile"),GEMINI_API_KEY:z.string().optional(),GEMINI_MODEL:z.string().default("gemini-2.5-flash"),OPENROUTER_API_KEY:z.string().optional(),OPENROUTER_MODEL:z.string().default("openrouter/free"),AI_MAX_HISTORY:z.coerce.number().min(4).max(100).default(30),AI_SYSTEM_PROMPT:z.string().default("Você é o ZipZapBot. Responda de forma curta e útil."),YTDLP_PATH:z.string().optional(),MAX_MEDIA_MB:z.coerce.number().default(45),DATA_DIR:z.string().default("./data")});
export const config=schema.parse(process.env);
