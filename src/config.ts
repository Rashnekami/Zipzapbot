import "dotenv/config";
import { z } from "zod";
const bool=z.string().default("true").transform(v=>v.toLowerCase()==="true");
const schema=z.object({BOT_NAME:z.string().default("ZipZapBot"),PREFIX:z.string().default("!"),PORT:z.coerce.number().default(8123),TIMEZONE:z.string().default("America/Sao_Paulo"),GROUPS_ENABLED:bool,PANEL_USER:z.string().min(1),PANEL_PASSWORD:z.string().min(10),AI_PROVIDER_ORDER:z.string().default("gemini,openai"),OPENAI_API_KEY:z.string().optional(),OPENAI_MODEL:z.string().default("gpt-5.6-luna"),OPENAI_TRANSCRIBE_MODEL:z.string().default("gpt-4o-mini-transcribe"),GROQ_API_KEY:z.string().optional(),GROQ_MODEL:z.string().default("llama-3.1-8b-instant"),GEMINI_API_KEY:z.string().optional(),GEMINI_MODEL:z.string().default("gemini-2.5-flash-lite"),OPENROUTER_API_KEY:z.string().optional(),OPENROUTER_MODEL:z.string().default("openrouter/free"),AI_MAX_HISTORY:z.coerce.number().min(4).max(100).default(30),AI_SYSTEM_PROMPT:z.string().default("Seja divertido, natural e acompanhe o clima do grupo."),WEB_SEARCH_ENABLED:bool,WEB_SEARCH_MAX_RESULTS:z.coerce.number().min(1).max(10).default(5),BOT_MOOD_ENABLED:bool,BOT_GRUDGE_MINUTES:z.coerce.number().min(1).max(1440).default(30),BOT_IRRITATION_THRESHOLD:z.coerce.number().min(2).max(20).default(5),BOT_FORGIVENESS_CHANCE:z.coerce.number().min(0).max(100).default(55),AUDIO_TRANSCRIPTION_ENABLED:bool,AUDIO_MAX_MB:z.coerce.number().min(1).max(25).default(15),YTDLP_PATH:z.string().optional(),MAX_MEDIA_MB:z.coerce.number().default(45),DATA_DIR:z.string().default("./data")});
export const config=schema.parse(process.env);
