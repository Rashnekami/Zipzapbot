import express from "express";
import QRCode from "qrcode";
import {config} from "./config.js";
let qr:string|undefined;let status="iniciando";
export function updatePanel(next:{qr?:string;status?:string}){if(next.qr!==undefined)qr=next.qr;if(next.status)status=next.status}
function auth(req:express.Request,res:express.Response,next:express.NextFunction){const header=req.headers.authorization;if(header?.startsWith("Basic ")){const [u,p]=Buffer.from(header.slice(6),"base64").toString().split(":");if(u===config.PANEL_USER&&p===config.PANEL_PASSWORD)return next()}res.setHeader("WWW-Authenticate",'Basic realm="ZipZapBot"');res.status(401).send("Autenticação necessária")}
export function startPanel(){const app=express();app.get("/health",(_q,r)=>r.json({ok:true,status}));app.get("/",auth,async(_q,r)=>{const image=qr?await QRCode.toDataURL(qr):"";r.send(`<!doctype html><html><meta charset="utf-8"><meta name="viewport" content="width=device-width"><title>ZipZapBot</title><style>body{font-family:system-ui;background:#08111f;color:#eaf2ff;display:grid;place-items:center;min-height:100vh}.card{background:#111d30;padding:32px;border-radius:20px;text-align:center;box-shadow:0 20px 60px #0008}img{width:290px;background:white;padding:12px;border-radius:12px}.ok{color:#46e39a}</style><div class="card"><h1>ZipZapBot</h1><p>Status: <b class="ok">${status}</b></p>${image?`<img src="${image}" alt="QR Code"><p>Escaneie no WhatsApp → Aparelhos conectados.</p>`:"<p>QR indisponível. Se já conectou, o bot está autenticado.</p>"}</div><script>setTimeout(()=>location.reload(),10000)</script></html>`)});app.listen(config.PORT,"0.0.0.0",()=>console.log(`Painel na porta ${config.PORT}`))}
